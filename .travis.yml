sudo: required
services:
  - docker

before_install:
  - docker build -t bluefroakie/docker-react -f Dockerfile.dev . # This might be my github username instead of my docker username

script:
  - docker run -e CI=true bluefroakie/docker-react npm run test # Same thing